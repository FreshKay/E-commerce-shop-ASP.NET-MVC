@model ShopSite.ViewModels.BasketViewModel
@{
    ViewBag.Title = "Cart";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h2>Basket <span id="basket-count">(@Html.Action("GetBasketQuantity", "Basket"))</span></h2>

<div class="container">
    <div class="check">
        <div class="col-md-3 cart-total">
            <a class="continue" href="@Url.Action("Index", "Home")">Continue shopping</a>
            <div class="price-details">
                <h3>Price Details</h3>
                <span>Total</span>
                <span class="total-value">@String.Format("{0:0.##}", Model.PriceSum) zł</span>
                <span>Discount</span>
                <span class="total1">---</span>
                <span>Delivery Charges</span>
                <span class="total1">---</span>
                <div class="clearfix"></div>
            </div>
            <ul class="total_price">
                <li class="last_price"> <h4>TOTAL</h4></li>
                <li class="last_price"><span class="total-value">@String.Format("{0:0.##}", Model.PriceSum) zł</span></li>
            </ul>
            <div class="clearfix">
            </div>

            <div class="clearfix"></div>
            @if (Model.PriceSum > 0)
            {
                <a class="order" id="cart-pay" href="@Url.Action("Pay", "Basket")">Place Order</a>
            }
            <div class="total-item">
                <h3>OPTIONS</h3>
                <h4>COUPONS</h4>
                <a class="cpns" href="#">Apply Coupons</a>
                <p><a href="#">Log In</a> to use accounts - linked coupons</p>
            </div>
        </div>
        <div class="col-md-9 cart-items">
            <h1>My Shopping Bag </h1>

            @foreach (var basketStatus in Model.BasketStatus)
            {
                <div class="cart-header" id="in-bucket-@basketStatus.Item.ItemId">
                    @*<div class="close1">*@<a class="deleteItem" data-id="@basketStatus.Item.ItemId">lllll</a>@*</div>*@
                    <div class="cart-sec simpleCart_shelfItem">
                        <div class="cart-item cyc">
                            <img src="@Url.Content("~/Content/images" + basketStatus.Item.ItemPicture)" class="img-responsive" alt="" />
                        </div>
                        <div class="cart-item-info">
                            <h3><a href="@Url.Action("Details", "Items", new { id = basketStatus.Item.ItemId })"> @basketStatus.Item.ItemName </a><span>@basketStatus.Item.ItemProducer</span></h3>
                            <ul class="qty">
                                <li><p>Qty : <span id="cart-quantity-@basketStatus.Item.ItemId">@basketStatus.Quantity</span></p></li>
                                <li><p>Cost: <span>@String.Format("{0:0.##}", basketStatus.Value) zł</span></p></li>
                                @*<li><p><a class="deleteItem" data-id="@basketStatus.Item.ItemId">Delete</a></p></li>*@
                            </ul>
                        </div>
                        <div class="clearfix"></div>

                    </div>
                </div>
            }

            <div id="empty-bucket" @if (Model.BasketStatus != null && Model.BasketStatus.Count > 0) { <text> class="hidden" </text>  }>
                <td>Your cart is empty.</td>
            </div>

        </div>
        <div class="clearfix"> </div>
    </div>
</div>
@section Scripts{
    
    @System.Web.Optimization.Scripts.Render("~/bundles/jquery")

    <script type="text/javascript">
        $(function () {
            $(".deleteItem").click(function () {

                var itemToDelete = $(this).attr("data-id");

                if (itemToDelete != '') {
                    $.post("/Basket/Delete", { "itemId": itemToDelete },
                        function (response) {
                            if (response.QuantityToDelete == 0) {
                                $('#cart-header-' + response.ItemIdToRemove).fadeOut('slow', function () {
                                    if (response.BasketQuantity == 0) {
                                        $("#empty-bucket").removeClass("hidden");
                                    }
                                });
                            } else {
                                $('#cart-quantity-' + response.ItemIdToRemove).text(response.QuantityToDelete);
                            }

                            if (response.BasketQuantity == 0) {
                                $('#cart-pay').addClass('hidden');
                                $('#total-value').addClass('invisible');
                            }
                            $('#total-value').text(response.BucketValue);
                            $('#basket-count').text(response.BucketQuantity);
                        });
                    return false;
                }
            });
        });
    </script>
@*<script type="text/javascript">
        $(function () {
            $(".usunKurs").click(function () {

                var recordDoUsuniecia = $(this).attr("data-id");

                if (recordDoUsuniecia != '') {
                    $.post("/Koszyk/UsunZKoszyka", { "kursId": recordDoUsuniecia },
                        function (response) {
                            if (response.IloscPozycjiUsuwanej == 0) {

                                $('#koszyk-wiersz-' + response.IdPozycjiUsuwanej).fadeOut('slow', function () {
                                    if (response.KoszykIloscPozycji == 0) {
                                        $("#koszyk-empty-message").removeClass("hidden");
                                    }
                                });
                            } else {
                                $('#koszyk-ilosc-pozycji-' + response.IdPozycjiUsuwanej).text(response.IloscPozycjiUsuwanej);
                            }

                            if (response.KoszykIloscPozycji == 0) {
                                $('#koszyk-button-zaplac').addClass('hidden');
                                $('#CenaCalkowita').addClass('invisible');
                            }
                            $('#calkowita-cena-wartosc').text(response.KoszykCenaCalkowita);
                            $('#koszyk-naglowek-ilosc-elementow').text(response.KoszykIloscPozycji);
                        });
                    return false;
                }
            });
        });
</script>*@
}
